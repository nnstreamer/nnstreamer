subdir('join')
subdir('nnstreamer')
if mqtt_support_is_available
  subdir('mqtt')
endif
if nnstreamer_edge_support_is_available
  subdir('edge')
endif
if datarepo_support_is_available
  subdir('datarepo')
endif
