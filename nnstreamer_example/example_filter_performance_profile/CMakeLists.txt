SET(EXAMPLE_APP_NAME "nnstreamer_example_filter_performance_profile")
ADD_EXECUTABLE(${EXAMPLE_APP_NAME} ${EXAMPLE_APP_NAME}.c)

TARGET_LINK_LIBRARIES(${EXAMPLE_APP_NAME} ${apppkgs_LIBRARIES})
TARGET_INCLUDE_DIRECTORIES(${EXAMPLE_APP_NAME} PUBLIC ${apppkgs_INCLUDE_DIRS})
TARGET_COMPILE_OPTIONS(${EXAMPLE_APP_NAME} PUBLIC ${apppkgs_CFLAGS_OTHER})

IF (INSTALL_EXAMPLE_APP)
	INSTALL(TARGETS ${EXAMPLE_APP_NAME} RUNTIME DESTINATION ${EXEC_PREFIX})
ENDIF (INSTALL_EXAMPLE_APP)
